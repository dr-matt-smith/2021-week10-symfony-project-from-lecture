{% extends 'base.html.twig' %}

{% block title %}Bed{% endblock %}

{% block body %}
    <h1>Bed</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bed.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ bed.name }}</td>
            </tr>
            <tr>
                <th>Size</th>
                <td>{{ bed.size }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ bed.price }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>
                    {{ bed.image }}
                <br>
                    <img src="/images/{{ bed.image }}">
                </td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('bed_index') }}">back to list</a>

    <a href="{{ path('bed_edit', {'id': bed.id}) }}">edit</a>

    {{ include('bed/_delete_form.html.twig') }}
{% endblock %}
