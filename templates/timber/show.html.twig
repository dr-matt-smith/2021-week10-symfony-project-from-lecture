{% extends 'base.html.twig' %}

{% block title %}Timber{% endblock %}

{% block body %}
    <h1>Timber</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ timber.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ timber.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('timber_index') }}">back to list</a>

    <a href="{{ path('timber_edit', {'id': timber.id}) }}">edit</a>

    {{ include('timber/_delete_form.html.twig') }}
{% endblock %}
